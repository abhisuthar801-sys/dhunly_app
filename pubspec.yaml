name: dhunly_pro
description: "Dhunly Music App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  http: ^1.1.0

flutter:
  uses-material-design: true
  # Maine assets wali line hata di hai taaki error na aaye
