name: dhunly_pro
version: 1.0.2+3 # Version badhata rehna har baar

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^5.2.1
  http: ^1.1.0

dev_dependencies:
  flutter_launcher_icons: "^0.13.1"

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/logo.png"

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
